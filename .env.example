# Client-side (prefix with VITE_ if used in the browser)
# (No Airtable secrets should be exposed to the client.)
# VITE_SOME_PUBLIC_KEY=your_public_value

# Server-side Airtable settings (DO NOT commit real secrets)
# These should be configured in your hosting platform (Vercel/Netlify) as environment variables.
AIRTABLE_API_KEY=key_your_airtable_api_key_here
AIRTABLE_BASE_ID=app_your_base_id_here
AIRTABLE_PRODUCTS_TABLE=Products

# Notes:
# - Do NOT prefix server-side secrets with VITE_. Only variables accessible to the browser should use the VITE_ prefix.
# - On Vercel add these to Project > Settings > Environment Variables (Production/Preview/Development).